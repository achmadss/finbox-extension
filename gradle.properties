org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.parallel=true
org.gradle.caching=true

# Version of com.github.achmadss.finbox-android:extension-api to compile against.
# Use a release tag or a finbox-android commit hash; JitPack resolves both.
finbox.apiVersion=1.1
