org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
org.gradle.parallel=true
org.gradle.caching=true

# Version of com.github.achmadss:finbox-android to compile
# against. A finbox-android release tag or commit hash; JitPack resolves both
# and builds it on demand, so nothing has to be published by hand.
finbox.apiVersion=1.4
